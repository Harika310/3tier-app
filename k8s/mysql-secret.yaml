apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
  namespace: expense
type: Opaque
stringData:
  root-password: "rootpassword"   # replace before applying (or leave and set via kubectl create secret)
  user-password: "apppassword"
